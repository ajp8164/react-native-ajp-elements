# `@react-native-ajp-elements/common`

Types shared across all @react-native-ajp-elements packages.

## Installation

Open a Terminal in your project's folder and run,

```sh
npm add @react-native-ajp-elements/common
```
